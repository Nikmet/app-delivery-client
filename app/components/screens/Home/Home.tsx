import { FC } from "react";
import { Text, View } from "react-native";

export const Home: FC = () => {
    return (
        <View>
            <Text></Text>
        </View>
    );
};

export default Home
